# TODO (2026-01-25)

## Vulkan decode backend (Stage 1–2)
- [x] Add metrics fields: decode/op/fft requested vs used; `gpu_hotloop_active`; strict/permissive fallback switch.
- [x] Implement Stage 1 GPU low-pass decode: persistent buffers, scatter low-k, vkFFT IFFT, timings, optional readback only.
- [x] Implement Stage 2 mask on GPU: smooth → ternary threshold → majority (iterative dispatch), int8 mask/support (combine/apply still CPU-side for residual synthesis).
- [x] Wire `--decode-backend {cpu,vulkan}` + strict/permissive flag consistently into `perf_kernel.py` and `run_v4_snapshots.py`; log stage timings.
- [ ] Add parity/smoke tests: CPU vs Vulkan low-pass MAE/energy/enstrophy + hash/log checks; ensure kernel-only perf path unchanged.

## Misc cleanup
- [ ] Decide fp32 vs fp64 decode default on AMD; document chosen policy.
- [ ] Choose location/policy for compiled `.spv` artifacts (single source of truth).

## Docs
- [x] Add `docs/overview.md` quickstart + runner map + backend notes.
- [x] Refresh `README.md` with runner pointers and utility scripts list.

## 3D NS truth bridge
- [x] Add CPU-first `scripts/make_truth_3d.py` that emits 3D periodic incompressible velocity/vorticity truth NPZ artifacts.
- [x] Add opt-in SPV/vkFFT 3D backend using vendored `dashiCORE` Vulkan infrastructure; keep CPU default and fail-fast GPU semantics.
- [x] Run GPU `make_truth_3d.py --backend gpu` smoke on target RADV/RX580 host with explicit ICD; validated zero-step and two-step N=16 artifacts.
- [x] Add fp64 Vulkan/vkFFT probe and diagnostic-only spectral backend for CPU/GPU harness parity; RX580/RADV reports `shaderFloat64=true`, complex128 vkFFT round-trip max error `~1.4e-15`, and N32 harness active-row `Q_K` parity `~3e-14` relative.
- [x] Add Sprint 49 material-parent producer `scripts/ns_material_parent_summary.py` for GPU truth artifacts. Current backend `gpu_spectral_gradient_cpu_packets` uses Vulkan/vkFFT fp64 spectral derivatives for stretch state and CPU packet matching/bin reduction.
- [x] Run Sprint 49 GPU material-parent batches for N32/N64 seed0/seed1. Outcome: true-new source is zero across all four runs; tracking is small when present; weighted cross-shell source dominates and routes to `ADJACENT_PACKET_THEOREM_INSUFFICIENT`.
- [x] Add Sprint 50 full ternary cross-shell matrix producer `scripts/ns_ternary_cross_shell_matrix.py`. The six-run N32/N64 batch routes as `CROSS_PLUS_FROM_MINUS_DOMINATES`; source kind is controlled by `parent_relation` plus shell delta, while transition entries use `parent_state -> child_state`.
- [x] Add Sprint 51 signed ternary flip producer `scripts/ns_signed_ternary_flip_audit.py`. The six-run N32/N64 batch routes as `NO2CYCLE_FAILS`; raw minus-to-plus is largely balanced by plus-to-minus, but the v1 packet-ID no-2-cycle proxy reports persistent sign-cycle failures.
- [x] Add Sprint 52 material source / no-2-cycle amplitude producer `scripts/ns_sprint52_material_no2cycle_audit.py`. The six-run N32/N64 batch routes as `MATERIAL_SOURCE_GATE_CLOSED_NO2CYCLE_AMPLITUDE_BLOCKED`: material true-new source is absent, while no-2-cycle amplitude remains blocked under the v1 material-packet proxy.
- [x] Add Sprint 53 no-2-cycle physical amplitude producer `scripts/ns_sprint53_no2cycle_physical_amplitude_audit.py`. The six-run N32/N64 batch routes as `MATERIAL_SOURCE_GATE_CLOSED_PHYSICAL_NO2CYCLE_AMPLITUDE_BLOCKED`: material true-new source remains absent, but the material net-residue physical-amplitude proxy reports only `0.3423412506059137` small failing cycles and `sigma_physical_cycle_fit = -1.1215088689186317`.
- [x] Add Sprint 54 no-2-cycle resolution/cadence producer `scripts/ns_sprint54_no2cycle_resolution_cadence_audit.py`. The six-run N32/N64 batch routes as `NO2CYCLE_PROXY_OVERCONSERVATIVE_STRETCH_SMALL`: shell/time direct-stretch small fraction is `0.9751575375666505`, while the material mass proxy remains bad and cadence/shell-boundary/packet-local stretch checks remain open.
- [x] Add Sprint 55 Lagrangian accumulated stretch-action producer `scripts/ns_sprint55_lagrangian_stretch_action_audit.py`. The six-run N32/N64 batch routes as `LAGRANGIAN_STRETCH_ACTION_SMALL_DIAGNOSTIC`: accumulated shell-lineage action demotes color-string churn, with `action_small_fraction = 0.9985242030696576` and `dangerous_lineage_count = 5`, but `sigma_action_fit = -0.5102412568825301` leaves weighted action summability open and packet-local masks remain unavailable.
- [x] Add Sprint 56 packet-local accumulated stretch-action producer `scripts/ns_sprint56_packet_local_stretch_action_audit.py`. The six-run N32/N64 batch routes as `PACKET_LOCAL_ACTION_SUMMABILITY_BLOCKED`: packet masks reconstruct from Sprint 49 `K_cell` geometry with `packet_local_available_fraction = 1.0`, but packet-local action-small fraction drops to `0.8108028335301063`, `dangerous_lineage_count = 641`, and `sigma_packet_local_action_fit = -0.4822543927548197`.
- [x] Add Sprint 57 vessel/action reconciliation producer `scripts/ns_sprint57_vessel_action_reconciliation_audit.py`. The six-run N32/N64 batch routes as `PACKET_ACTION_UNDERCOUNTS_COVERED_STRETCH`: raw packet positive stretch action under-reconstructs covered/global positive stretch (`epsilon_raw_positive_vs_covered = -0.8161321565334568`, `epsilon_raw_positive_vs_global = -0.9608719590659198`), while normalized packet positive action is inflated relative to normalized global action (`epsilon_normalized_positive_vs_global = 113.58553013012235`).
- [x] Add Sprint 58 normalized packet-action inflation producer `scripts/ns_sprint58_normalized_action_inflation_audit.py`. The six-run N32/N64 batch routes as `NORMALIZED_ACTION_NONADDITIVE_RATIO_INFLATION`: `sum_ratios_over_ratio_of_sums_covered = 4904.346096600663`, `sum_ratios_over_ratio_of_sums_global = 11471.817018880183`, and `low_enstrophy_denominator_fraction = 0.012394729693018202`.
- [x] Add Sprint 59 raw additive packet-stretch producer `scripts/ns_sprint59_raw_packet_stretch_action_audit.py`. It computes voxelwise raw positive/negative/net `omega dot S omega` packet actions and shell fits as the vessel-additive replacement for the Sprint 56 normalized-ratio summability object.
- [x] Add R/G/B packet-thread volume visualizer `scripts/ns_rgb_thread_volume_visualizer.py`. It rebuilds Sprint 49 `K_cell` packet masks, stores nil cells as NaN, renders plus/red, zero/green, minus/blue voxels at configurable opacity, emits GIF/WebM animations over all times, and can visualize Sprint 59 raw-action-derived trits. Generated sample outputs in `outputs/rgb_thread_volume_visuals/`.
- [x] Add Sprint 60 BT/smoothed shell raw-action reassignment audit `scripts/ns_sprint60_raw_action_reassignment_audit.py`. The six-run N32/N64 batch routes as `RAW_ACTION_REASSIGNMENT_FLAT_BLOCKED`: Euclidean, smoothed, and provisional BT/ultrametric assignments conserve raw action, but all fitted `sigma_raw_action_fit` values remain near flat (`-0.02318`, `-0.02256`, `-0.03123`) and all Clay/NS promotion flags remain false.
- [x] Add Sprint 61 raw-red direction-coherence anatomy producer `scripts/ns_sprint61_raw_red_direction_coherence_audit.py`. The six-run N32/N64 batch routes as `RAW_RED_DIRECTION_INCOHERENT_CONCENTRATION_BLOCKED`: among 1471 selected high raw-red packets, 1451 are incoherent under the current proxy (`incoherent_packet_fraction = 0.9864038069340585`, `direction_coherence_mean_selected = 0.5295134456542706`), so the immediate DASHI/CFM coherent-tube rescue is blocked on current evidence.
- [x] Add Sprint 62 direction-coherence robustness producer `scripts/ns_sprint62_direction_coherence_robustness_audit.py`. The current Sprint 61 packet anatomy routes as `DIRECTION_COHERENCE_INCOHERENCE_ROBUST_ON_AVAILABLE_DATA` across tested top-fraction/coherence-threshold settings. Follow-up GPU ladder runs now include N128 seed0 (`sigma_raw = -0.015597324361506952`, Sprint 61 incoherent, Sprint 62 robust) and dense-cadence N64 seed0 save-every=2 (`sigma_raw = -0.03440204529464993`, Sprint 61 incoherent, Sprint 62 robust).
- [x] Optimize Sprint 61 direction-coherence processing by caching frame-wide vorticity unit fields and direction-gradient magnitudes once per saved frame instead of recomputing `np.gradient` per selected packet. N128 seed0 Sprint 61 rerun completed in ~24s with the same route decision.
- [x] Add Sprint 63 cross-shell replenishment contractivity producer `scripts/ns_sprint63_cross_shell_replenishment_contractivity_audit.py`. It joins Sprint 49 parent edges to Sprint 59 raw-action ledgers, preserves multi-parent edges, compares child raw action against credited parent raw-action budgets, and routes the remaining DASHI-native NS theorem fork without any Clay/NS promotion. Current results route as `CROSS_SHELL_REPLENISHMENT_MIXED` but strongly noncontractive: six-run N32/N64 `noncontractive_edge_fraction = 0.9532640658694373`, N128 seed0 `0.9987681013676589`, and dense N64 seed0 `0.9901806026277914`.
- [x] Record the post-Sprint 63 NS source-budget verdict: normalized packet action, raw action shell summability, action-preserving shell reassignment, raw-red direction coherence, and simple parent-budget cross-shell contractivity are all diagnostically blocked on current evidence. The NS lane should switch to a critical concentration norm or pivot away from the source-budget route.
- [x] Add Sprint 64 CKN/local critical concentration producer `scripts/ns_sprint64_ckn_local_critical_concentration_audit.py`. It computes velocity-only local `L3` concentration from existing truth `velocity_snapshots`, explicitly marks pressure reconstruction as missing when no pressure array is present, classifies `grounded/plateau/ascended` Overflow states, and keeps all Clay/NS promotion flags false. Current six-run N32/N64 result routes as `LOCAL_CRITICAL_CONCENTRATION_PRESSURE_RECONSTRUCTION_MISSING`, with `ascended_fraction = 0.9642628205128205`; N128 seed0 reports `0.8740818643162394`, and dense N64 seed0 reports `0.926542577413479`, all under the diagnostic `epsilon_critical = 0.01`.
- [x] Add Sprint 65 pressure reconstruction producer `scripts/ns_sprint65_pressure_reconstruction.py`. It derives zero-mean periodic pressure snapshots from `velocity_snapshots`, appends `pressure_snapshots` to pressure-augmented truth NPZs, emits Poisson residual/gauge diagnostics, and reruns Sprint 64 on the pressure-present artifacts without any Clay/NS promotion. Six-run N32/N64 pressure reconstruction reports `max_poisson_relative_residual_rms = 3.5409688067143674e-16`; the pressure-present Sprint 64 rerun routes as `LOCAL_CRITICAL_CONCENTRATION_MIXED` with `ascended_fraction = 0.9890491452991453`. N128 seed0 and dense N64 seed0 also route mixed.
- [x] Add Sprint 66 CKN r-sweep calibration producer `scripts/ns_sprint66_ckn_r_sweep_calibration.py`. It consumes pressure-present Sprint 65 truth artifacts, optionally uses Sprint 59 raw-action packet rows as hotspot seeds, computes candidate-centred scale-normalized `C(r) = r^-2 integral_Q (|u|^3 + |p|^(3/2)) dx dt` across multiple radii/epsilons, emits trend labels and route decisions, and keeps all CKN theorem / Clay / NS promotion flags false. Current results route as `CKN_R_SWEEP_DECAYS_UNDER_ZOOM`: six-run N32/N64 has `decaying_hotspot_count = 60`, `concentrating_hotspot_count = 0`, and `ascended_fraction = 0.43666666666666665`; N128 seed0 has `ascended_fraction = 0.116`; dense N64 seed0 has `ascended_fraction = 0.125`.
- [x] Add Sprint 67B CKN uniformity producer `scripts/ns_sprint67_ckn_uniformity_audit.py`. It consumes pressure-present truth artifacts plus optional Sprint 64 fixed-block ascended cylinders, computes pressure-inclusive `C(r)` r-sweeps at each candidate centre, emits by-cylinder and by-cluster ledgers, classifies uniform decay / plateau / concentration / pressure-dominated routes, and keeps all CKN theorem / Clay / NS promotion flags false. Bounded six-run replay routes as `CKN_UNIFORM_DECAY_SUPPORTED` with `1536/1536` decaying cylinders, `120` clusters, `0` flat/concentrating cylinders, `persistent_cluster_count = 0`, and `pressure_fraction_max = 0.13074814940071125`.
- [ ] Extend the Sprint 49→59→61→62 ladder to N1024 as feasible and refine the direction-coherence proxy toward a theorem-grade CFM quantity before treating the incoherence result as continuum evidence.
- [ ] Compare longer GPU rollout parity against CPU before using GPU truth artifacts as primary harness evidence.
- [ ] Run external physical bridge harness against `outputs/truth3d/ns3d_N32_seed0.npz` and record whether `C_K` stays bounded.
- [ ] Move Sprint 49 packet-bin accumulation onto GPU so the producer reads back `stretch_state` or compact packet-bin accumulators instead of full derivative tensors.
- [ ] Add optional lineage labels (`ternary_label_snapshots`, `packet_id_snapshots`, `shell_id_snapshots`) after the 3D bridge falsification path is working.
